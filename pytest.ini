[pytest]
# Pytest configuration for LifeFinances

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

norecursedirs = .git .venv __pycache__ .pytest_cache .claude

# Additional pytest options
addopts =
    --strict-markers
    --tb=short
    --disable-warnings
