name: Main CI

on:
  push:
  pull_request:

jobs:
  docker_test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Print Env
      run: printenv

    - name: Run Tests
      run: make