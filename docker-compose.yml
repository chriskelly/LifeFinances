version: "3"

services:
  life_finances:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/src:ro
      - ./tests:/tests:ro
      - ./config.yml:/config.yml:ro
    ports:
      - "3000:3000"
    entrypoint:
      - python
      - /run.py
